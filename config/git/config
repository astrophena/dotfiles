[log]
	mailmap = true
[credential]
	# Used on Windows.
	helper = wincred
	# Used on other platforms.
	helper = store
[user]
	name = Ilya Mateyko
	email = gitlab@inbox.astrophena.me
[alias]
	pushall = !git remote | xargs -L1 git push --all
