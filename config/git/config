[log]
	mailmap = true
[credential]
	helper = manager# Used on Windows.
	helper = store  # Used on other platforms.
[user]
	name = Ilya Mateyko
	email = gitlab@inbox.astrophena.me
[alias]
	pushall = !git remote | xargs -L1 git push --all
