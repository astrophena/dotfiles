DOTFILES_DIRS="$HOME/src/dotfiles"
EXCLUDES="*.md docs install.sh"
UNDOTTED="bin"

TAGS="bash curl git htop ruby ssh vim"

if [ -f /google/devshell/bashrc.google ]; then
  TAGS="cloudshell ${TAGS}"
fi

if [ "$(uname -o)" = "Android" ]; then
  TAGS="termux ${TAGS}"
fi
