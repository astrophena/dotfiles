DOTFILES_DIRS="$HOME/s/dotfiles"
EXCLUDES="*.md docs install.sh"
UNDOTTED="bin"

TAGS="base bash curl git htop ssh vim"

if command -v neofetch >/dev/null 2>&1; then
  TAGS="neofetch ${TAGS}"
fi

if [ "$(uname -o)" = "Android" ]; then
  TAGS="termux ${TAGS}"
fi
